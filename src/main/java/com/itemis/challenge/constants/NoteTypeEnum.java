package com.itemis.challenge.constants;

public enum NoteTypeEnum {
    // Note defines an intergalactic unit
    D_INTERGALACTIC_UNIT,

    // Note defines the price for a given amount(expressed in intergalactic units) of a single item
    D_ITEM_PRICE,

    // Query to convert the intergalactic units to decimal
    Q_CONVERT_UNITS,

    // Query to calculate the price for a given amount (expressed in intergalactic units) of a single item
    Q_ITEM_PRICE,

    // Note contents are not valid
    NOT_VALID
}
